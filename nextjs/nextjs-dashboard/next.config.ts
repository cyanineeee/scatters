import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // because the nextjs version is not suitable, which requires nextjs c 
  // experimental : {
  //   ppr:'incremental',
  // }
};

export default nextConfig;
